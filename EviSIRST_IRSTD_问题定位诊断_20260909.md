# IRSTD 问题定位诊断：先定位，再裁决 CEL

日期：2026-09-09。状态：诊断已完成，全部训练与 CEL 自动队列保持停止。本报告不是新模型结果，也不授权自动训练。

> 发布边界：本文所依据的原始诊断 artifact 保留在外部本地工作树，未随
> `EviSIRST_main` 公开；本仓库仅发布带证据边界的结论摘要，干净克隆无法独立重放该诊断。

## 1. 当前裁决

此前从“共同检出目标填充不足”推进到“最后一级 CCA 应是主要修改位置”，证据链并不充分。现在可以确认输出错误的组成，并检验一个固定权重干预方向；仍不能声称已经定位训练退化的唯一网络根因。

主要结论：

1. CAST 的分割缺口主要体现为目标像素覆盖不足，净覆盖退化集中于面积大于25像素的对象，不能只按极小目标漏检解释。
2. A1 的 best_mIoU 没有增加全局目标覆盖；best_Pd 确有覆盖改善，但背景误报代价更大。两种角色不能合并为“修复成功”。
3. 最后 CCA 放宽存在目标恢复响应，但缺乏足够的选择性；本轮两种预先固定的放宽都未提升 mIoU。最突出的困难样本在六个监督头均已呈低置信度，不符合“前级已分割完整、仅末级丢细节”的观测模式。
4. CAST 的低 Fa 不能直接解释为更强的背景抑制。粗 C 角色在恢复目标像素和新增误报像素上都很高，也不能直接当作像素级恢复资格。

因此：**CEL 保留为未验证候选，暂不进入长训；先检查跨尺度读出/表示、目标尺寸与形状的覆盖，以及局部证据能否区分目标恢复和背景外溢，再决定修改位置。** 这不是判定所有末级细化必然无效，也不是凭本轮诊断提出另一套模块。

## 2. 范围与完整性

- 原 `img_idx` train/test 不变；仅访问原 IRSTD test 的201张图，297个 GT 对象、14,534个目标像素。不新增验证集。
- 本轮没有训练、反向传播、优化器更新、阈值搜索、checkpoint 重选或新权重导出。固定阈值 `p>0.5`、GT `y>0.5`、8连通、质心匹配距离严格小于3像素；有效区域裁边、FP32 和正式 evaluator 均保持原口径。
- 复用旧 SCTransNet e713、CAST best_mIoU e700 / best_Pd e531 几何 JSON。没有重新跑三个旧权重的整套几何诊断。
- 新增 A1 e778/e503 各一次201图前向，同时记录六头；CAST e700 为末级干预运行一次201图原生前向，每图仅重算三种固定后缀。正常 CAST 输出与旧逐图几何完全相同。
- 所有模块保持 eval；内部 `mode='train'` 只用于返回六个读出，不更新 BN。正式输出始终为第六个 `out`，不切换为融合头 `d0`。
- A1 两角色和 CAST 干预产物均 `all_gates_pass=true`。CAST λ=0 的 out/d0 在201图上逐元素相同；模型/BN状态、源文件、权重文件、test输入前后不变，hooks 清除。138个冻结 Python 科学/运行源文件核验通过。
- 仅使用物理 GPU3；结束后 GPU2/3 无计算进程。本项目 A1 两服务及 CEL 队列均 `inactive/dead`、`MainPID=0`。其他用户进程未操作。
- A1 停于 IRSTD e874、NUDT e641，不能写成已完成1000轮；本报告只分析 IRSTD 已选权重。未来正式协议仍为 seed42、1000 epoch、第500轮起逐轮 test、两类 best 分开完整报告。

这些是已用于选模的测试集上的事后诊断，不是独立、无偏的最终泛化证据。本轮使用实验设计技能把观察、干预和原因假设分开，并预先冻结干预条件；没有以诊断结果产生第三种选模规则。

## 3. 分开报告两类权重

mIoU、nIoU、F1、Pd 为 %；Fa 为 ×10⁻⁶。各行均来自一个真实权重。

| 模型与角色 | Epoch | mIoU ↑ | nIoU ↑ | F1 ↑ | Pd ↑ | Fa ↓ |
|---|---:|---:|---:|---:|---:|---:|
| SCTransNet，既有参考 | 713 | 67.7657 | 67.1461 | 80.7862 | 93.2660 | 20.8005 |
| 无 WDS CAST，best_mIoU | 700 | 66.8272 | 66.8399 | 80.1155 | 95.2862 | 11.6908 |
| A1，best_mIoU，停训时已选 | 778 | 66.9089 | 67.1578 | 80.1741 | 93.2660 | 17.4413 |
| 无 WDS CAST，best_Pd | 531 | 63.5964 | 64.8090 | 77.7479 | 96.6330 | 35.0535 |
| A1，best_Pd，停训时已选 | 503 | 62.9537 | 64.7982 | 77.2658 | 96.9697 | 40.3675 |

### 3.1 全局像素与 Fa 必须分开

TP/FN 是正确覆盖/未覆盖的 GT 像素；FP 是全局真实背景预测为目标的像素。原 Fa 分子则是**未匹配预测连通域的全部面积**，可能包含 GT 重叠像素。

| 权重 | TP | FP | FN | Fa分子中GT重叠 | Fa分子中真实背景 |
|---|---:|---:|---:|---:|---:|
| SCTransNet e713 | 11,386 | 2,268 | 3,148 | 501 | 595 |
| CAST e700 | 11,237 | 2,281 | 3,297 | 2 | 614 |
| A1 e778 | 11,234 | 2,256 | 3,300 | 208 | 711 |
| CAST e531 | 11,275 | 3,195 | 3,259 | 208 | 1,639 |
| A1 e503 | 11,586 | 3,870 | 2,948 | 130 | 1,997 |

CAST e700 对 baseline：Fa 分子下降480 = GT重叠部分下降499 + 真实背景部分增加19；全局 FP 反而增加13。不能写成“背景误报像素减少480”。这只是解释原指标，不替换 Fa、不改匹配规则。

例如 XDU406：两模型都覆盖184个 GT 像素，FP 为46/47。相关预测组件与 GT 的质心距离从3.1790变为2.9720，跨过原3像素界限，匹配状态因此改变。Pd/Fa 的这类改善不能等同于全局分割质量改善。

### 3.2 尺寸与配对结果

CAST e700 相对 baseline 的目标覆盖净少149像素，其中 `>25 px` 桶少138，`10–25 px` 桶少13，`5–9 px` 桶多2，`1–4 px` 桶不变。`>25 px` 是本次既有尺寸桶名称，包含多种尺度，不意味着全部是语义上的“大目标”。

旧两模型共同检出的275个对象，匹配对缺失像素 +164、外溢 −110。匹配对缺失不是全局 FN；一个 GT 还可能被其他预测组件覆盖。

A1 同角色配对进一步显示：

- e778 对 CAST e700：全局 TP −3、FP −25。共同检出276个对象，GT覆盖 −14、匹配对缺失 +23、外溢 −54；新检1个、丢检7个。四组覆盖变化为共同 −14、新检 +17、丢检 −73、双方未检 +67，合计 −3。`>25 px` 覆盖已有 +21，但其余尺寸桶合计 −24，不能说 A1 所有尺度都退化。
- e503 对 CAST e531：全局 TP +311、FP +675。共同检出285个对象，GT覆盖 +274、匹配对缺失 −287、外溢 +298；新检3个、丢检2个。四组覆盖变化 +274、+3、−27、+61，合计 +311。恢复是真实的，但未控制误报代价。

## 4. 最后 CCA 是否是充分修改位置？

固定 CAST e700，令 x 为原高分辨率 skip，a 为原 CCA 输出，预先固定 `a_λ=a+λ(ReLU(x)−a)`。λ=0 是原生控制，0.25 是温和放宽，1 是完全绕过此通道门控；所有其他输入和后缀权重不变。

下表是**固定权重干预读数，不是三个新模型或新 best**。

| 条件 | mIoU | nIoU | F1 | Pd | Fa ×10⁻⁶ | 净TP变化 | 净FP变化 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 原生 / λ=0 | 66.8272 | 66.8399 | 80.1155 | 95.2862 | 11.6908 | 0 | 0 |
| λ=0.25 | 66.7308 | 66.9486 | 80.0462 | 95.6229 | 14.6135 | +200 | +324 |
| λ=1 | 64.7151 | 64.0613 | 78.5782 | 95.6229 | 28.1832 | +380 | +1,136 |

温和放宽：FN→TP 229、TP→FN 29；TN→FP 368、FP→TN 44。完全放宽：分别为523、143、1,303、167。因此不是单调“只补回目标”；特征放宽经原非线性后缀后也会丢失一部分已覆盖像素。

结论是：这里存在可恢复响应，但直接放宽不足以改善 mIoU/F1。温和条件的 nIoU/Pd 有提升，仍是权衡，不能概括为五项全坏。冻结干预有分布偏移，也不等于 CEL 的可训练、角色条件化操作，故不能直接宣判 CEL 必然失败。

### 4.1 最大困难样本不是只差最后一点边缘

XDU997 的 GT 为1065像素，原图中是可见的扩展飞机形状。它贡献 baseline/CAST e700/A1 e778 残余 FN 的31.42%/32.30%/30.67%，是共有困难样本，不是某一层造成三分之一错误的证明。没有删样本、改标签或以它重选权重。

- CAST e700：六个头在全部1065个 GT 像素上均 `p<0.1`，正常 out 最大概率仅0.0366。温和放宽仍覆盖0像素，完全放宽只覆盖1像素。
- A1 e778：六头覆盖为 `0/0/11/54/45/53`，正式 out 中968/1065个 GT 像素仍 `p<0.1`。
- A1 e503：六头覆盖为 `0/92/122/116/120/128`，正式 out 中893/1065个 GT 像素仍 `p<0.1`。

这不是“前级已经完整分割、末级才丢掉大量细节”的观测模式。辅助头有各自尺度、分类器和校准，不能反推上游特征完全没有信息，也不能仅凭头间对比确定编码器或路由是根因。

### 4.2 粗角色证据不能直接充当恢复掩码

温和干预的第1查询尺度中，恢复像素 FN→TP 的 C 概率均值为99.6939%；新增误报 TN→FP 的 C 均值也有98.0616%。另外三个查询尺度结果接近。这些是现有16×16 token 广播到像素位置的预测角色概率，不是 GT 占比。

因此“C 高就恢复”同时可能放大目标和其附近背景。均值相近本身不能证明两类完全不可区分，也未证明局部细化一定能区分；本轮没有拟合新分类器或调整角色阈值。CEL 的局部分配与均值保持需要面对这个限制，不能只凭角色名称宣称闭环有效。

## 5. 损失代码审计与下一步边界

A1 的 object 项先对每个增强后连通域内部像素取均值，再对 patch 内对象取均值，最后对 batch 取均值。它是 patch 内对象均权，不是全数据集对象均权，也不是按全局 FN 像素量分配修复预算。相同残差与 patch 目标数时，大对象的单像素权重更小；这提示与逐像素 global mIoU 可能存在目标错位，但不是已证明的性能根因。

Tail 对非空近环/远背景区域等质量取 top-k；它监督 `y==0` 的背景，不直接监督 Fa 的未匹配组件面积。两项共享现有预算，不存在发现的重复系数乘法；旧 RET 的50—130倍解码梯度和 H 1.93%不能套给当前 A1。本轮不重跑旧梯度或几何检查，不增加动态平衡器。

下一步应检验而非直接宣称：

1. 跨尺度响应不足与尺寸/形状覆盖：结合原训练分布与固定对象分层，区分表示未形成、读出不足和末级选择问题；不能只针对 XDU997 设计。
2. 已检目标填充与局部背景分离：需要证明某种局部证据对恢复像素和外溢像素有区分力，才决定角色细化是否有用。
3. 如证据最终指向损失预算，则修改预算；如指向特定融合环节，则最小改动该环节。不预先规定一定使用 CEL，不为凑创新点堆模块。

本轮到此停止诊断，保留全部结果与权重，不启动候选长训。

## 6. 可复查产物

- `DIAGNOSTIC_SCOPE.md`：执行前范围；
- `CCA_PROBE_PROTOCOL.md`：执行前 CCA 干预协议；
- `existing_geometry_decomposition.json`：复用旧几何的精确拆分；
- `a1_geometry.json`：A1 两角色、六头和逐图/逐对象配对；
- `cast_e700_cca_probe.json`：CAST 固定权重干预、像素转移与角色统计。

这些原始产物与诊断脚本位于未公开的外部工作树，本次仅保留文件标识以便发布方后续迁移；
JSON 内含来源、权重和数据身份记录，但未随本仓库提供。
