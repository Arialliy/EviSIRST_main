# A1 训练收尾与 CEL 定向优化方案

日期：2026-09-09。本文是独立研发记录，不是已经验证的论文结论。

执行状态更新（19:56）：用户要求停止 A1，IRSTD 与 NUDT 已分别停在最后保存的 874、641 epoch；CEL 自动接续队列也已暂停，且未启动正式训练。下文最初的“继续 A1 收尾后接续”安排仅作为被撤销的历史协议保留，不再执行。模型设计与已通过的检查保留，CEL 尚无正式训练结果。

诊断裁决更新（同日，优先于下文原设计假设）：用户要求先定位问题。新增两角色 A1 配对与固定 CAST e700 末级干预均已完成且通过复现/完整性检查。A1 e778 对 CAST e700 的 TP/FP 为 −3/−25；e503 对 CAST e531 为 +311/+675。CCA 温和放宽的 mIoU 为66.7308%，低于原66.8272%；完全放宽为64.7151%。XDU997 在 CAST 六头均表现低置信度，不能把主要瓶颈直接定位为最后 CCA。另已确认原 Fa 降低主要涉及未匹配组件内 GT 重叠计数变化，不能直接解释成更强背景抑制。**CEL 保持未验证候选、不得自动开训；先检查跨尺度响应与局部目标/背景区分。** 完整证据及边界见 [IRSTD 问题定位诊断](EviSIRST_IRSTD_问题定位诊断_20260909.md)。

## 1. 原执行决定与当前覆盖状态

- 原计划（已撤销）：GPU2 上 NUDT A1、GPU3 上 IRSTD A1 继续原有 1000 epoch 收尾，不改其源码、损失、seed、划分或选择规则；不使用 GPU1。
- 新模型以**无 WDS V3.4-CAST**为直接母体，不以 A1 的附加损失为母体，不加载旧模型权重。继承 CAST 的原始结构与初始化，在最后一级 CCA 中引入一个小型结构修改。
- 新候选暂定名为 `SCTransNet-C3-SBSC-V3.5-CEL`，方法标识 `sbsc_v35_cel`，禁止将其检查点混记成 CAST、V3.3 或 A1。它与较早但未实施的 V3.5-SOCRT 候选互斥；若后续恢复研究，必须先冻结唯一版本名，不得将两者合并为同一方法。
- CEL 暂释为 Conservative Evidence Lifting，中文“守恒式角色证据细化”。“守恒”仅指**预测角色概率在每个 token 对应像素块内的均值**，不是目标面积守恒、能量守恒或正确标签守恒。
- 开发位置：独立本地工作树（不随本仓库发布）。运行中的旧工程和主工程科学源码不改。
- 当前状态：独立本地工作树已完成实现和启动前检查，但队列已在正式训练前停止，且相关源码与完整证据未随本仓库发布。是否超越 baseline 必须等待真实长训，不能由结构设计或预检通过推出。

## 2. 已有证据：需要修复的是权衡，不是简单“损失不够强”

读取日志时 IRSTD A1 已完成 793/1000，NUDT A1 已完成 602/1000；这只是该时刻快照。下面 A1 的 best 是当时已完成 epoch 内的最优，不是完成 1000 epoch 协议的最终结果；停训至 e874 时，IRSTD 的两个 best epoch 仍为 e778/e503。

指标单位：mIoU、nIoU、F1、Pd 为 %，Fa 为 ×10⁻⁶。每一行完整来自同一权重，不跨权重拼指标。

| IRSTD 模型 | 权重角色 | Epoch | mIoU ↑ | nIoU ↑ | F1 ↑ | Pd ↑ | Fa ↓ |
|---|---|---:|---:|---:|---:|---:|---:|
| SCTransNet | 用户提供的复现参考权重 | 713 | 67.7657 | 67.1461 | 80.7862 | 93.2660 | 20.8005 |
| 无 WDS CAST | best_mIoU，已收尾 | 700 | 66.8272 | 66.8399 | 80.1155 | 95.2862 | 11.6908 |
| A1 | best_mIoU，IRSTD e793 诊断快照 | 778 | 66.9089 | 67.1578 | 80.1741 | 93.2660 | 17.4413 |
| 无 WDS CAST | best_Pd，已收尾 | 531 | 63.5964 | 64.8090 | 77.7479 | 96.6330 | 35.0535 |
| A1 | best_Pd，IRSTD e793 诊断快照 | 503 | 62.9537 | 64.7982 | 77.2658 | 96.9697 | 40.3675 |

A1 e778 对 CAST e700 的 mIoU、nIoU、F1 已分别回升约 0.0816、0.3179、0.0586 个百分点，但 Pd 下降 2.0202 个百分点、Fa 上升 5.7505。不能继续使用“A1 五项均低于 CAST”的旧判断，也不能称其全面更优。A1 尚未超过 SCTransNet 的 mIoU/F1。

对象统计：CAST e700 检出 283/297 个目标，A1 e778 为 277/297；未匹配预测像素分别为 616 和 919。这是**净差**，没有配对目标 ID 不能写成“同样的六个目标丢失”。

已有 CAST/SCTransNet 几何报告中，共同检出的 275 个目标上 CAST 多缺失 164 个像素、少外溢 110 个像素，只支持那一对旧权重存在填充不足的方向。不能把这一结果直接当成 A1 e778 的配对诊断。

### 不重复旧诊断，也不混淆旧 RET 与当前 A1

旧 RET 的解码层梯度 50—130 倍、H 权重保留 1.93%，不是当前 A1 的实测结论。当前 A1 没有新增 H 监督。

已有逐轮日志的新增实际梯度预算仍在工作：在 IRSTD 500—786、NUDT 500—599 的记录中，按样本量加权的已记录批次范数均值之比，gross/base 约为 0.20，net/base 约为 0.146，未出现预算下溢。这个统计不是全模型梯度比，也不是逐批比值的平均。

在 A1 的 logits 空间，目标项与背景项支持集不相交；net 小于 gross 可以来自范数几何，不能据此宣称两项梯度冲突。共享参数空间是否冲突需要另外证据。本轮不再增加动态梯度平衡器，不重跑已有完整几何、标签或 IO 诊断。

## 3. 问题—设计对应关系

**问题假设**：CAST 的 C/H/B 角色证据来自粗 token 网格，而最后一级原 CCA 主要是全局池化后的通道门控。目标与背景在同一粗 token 内混合时，角色证据没有直接规定最后一级高分辨率 skip 应在哪里恢复、在哪里抑制。这个结构事实可以核实；它是否解释性能瓶颈，需要实验验证。

**设计**：把现有最高分辨率查询分支的 C/H/B 路由先验，在其对应的 16×16 像素块内细化；局部空间差异来自同一级编码器 skip 与解码器特征。细化后的 C 只调节原 CCA 未接纳的正特征，H 只调节已经接纳的特征，B 作为剩余角色，不新增第三条修复分支。

因此不是增加边缘头、波形分支、第二套路由器或额外损失，而是闭合**已有角色路由 → 最终 skip 细化 → 原分割损失反向更新角色路由**这条路径。该设计是待检验的机制主张，不能预先称为首次提出。

## 4. 具体结构与约束

设最后一级编码器、解码器特征为 x、g，均为 32 通道。原 CCA 的输出记为 a，其计算顺序和参数原样保留。

1. x、g 各经 1×1 卷积投影到 8 通道，再经 SiLU，得到 t_x、t_g。
2. 拼接 t_x、t_g、|t_x−t_g|、t_x−AvgPool3(t_x)、t_g−AvgPool3(t_g)，共 40 通道。
3. 3×3 卷积 40→16、SiLU、1×1 卷积 16→2，产生 C 与条件 H 的空间分数。不增加监督头。
4. 现有路由 logits 的 softmax 给出每 token 的先验 (c,h,b)。先细化 C，再在剩余概率内细化 H，B=1−C−H。

对于每个 16×16 块，令 s_C=sigmoid(score_C)，μ_C=mean(s_C)：

```text
k_C = min(c/μ_C, (1−c)/(1−μ_C))
C   = c + k_C (s_C−μ_C)
R   = 1−C
q   = h/(h+b)
μ_H = mean(R·sigmoid(score_H)) / mean(R)
k_H = min(q/μ_H, (1−q)/(1−μ_H))
H   = R · [q + k_H (sigmoid(score_H)−μ_H)]
B   = R−H
```

实现包括分母保护、c=0/1 端点处理和浮点舍入保护。在浮点容差内检查：像素概率非负、C+H+B=1、每块均值等于对应先验。H 分数不改变 C。**这种均值保持与有界缩放是已知数学思路，不单独作为原创贡献。**

末级修正为：

```text
d   = ReLU(x)−a
out = a + gain_C · C · d − gain_H · H · a
```

gain_C、gain_H 均为 32 维可学习参数，初始化为 0，更新后投影到 [0,0.5]。局部分数网络使用非零随机初始化，避免将整个新支路同时零初始化而阻断学习。初始化和调用者 RNG 不被额外参数构造扰动。

理想算术下有 0.5a ≤ out ≤ ReLU(x)。这是**本层特征幅值界**，不是最终预测概率、Pd 或 Fa 的性能保证。零 gain 时输出等于原 CCA，必须用实际模型验证，而不只看公式。

新增 6,368 个参数；总计 11,336,556 个参数，521 个 state 项。继承 CAST 的 514 个 state 项在构造时逐字节一致；新增的 7 项具有独立 schema 与严格白名单。仅替换最后一级 CCA 与同拓扑的路由证据传递包装，其他层不改。

### 明确局限

- 若粗先验 c=0，细化不能凭空创造 C；单像素 C 还受该块总先验质量约束。原 CCA 主路径仍在，不能因此称目标被硬删除。
- H 的语义或空间定位错误时也可能压制真实目标特征；有界并不等于有益。
- 本版本是单模型、单一结构修改。若证据细化不能改善同 token 内定位，不通过继续叠模块或增大权重掩盖失败。
- 正式实现为 FP32。未验证的 AMP、任意 tile 比例、编译图兼容性不宣称支持。

## 5. 参考方法与创新边界

完整筛选记录在独立开发目录的 `docs/literature/`，区分已核对官方代码与仅有论文的来源。本地未公开模型名、指标与数据不作为联网检索内容。

| 参考 | 借鉴的设计原则 | 明确不照搬的内容 / 区别 |
|---|---|---|
| [SCTransNet，TGRS 2024](https://arxiv.org/abs/2401.15583) | 在既有跨尺度通道交互基础上找具体缺口 | 保留母体；不能把多尺度或空间通道融合本身算作新贡献 |
| [MSHNet，CVPR 2024](https://openaccess.thecvf.com/content/CVPR2024/papers/Liu_Infrared_Small_Target_Detection_with_Scale_and_Location_Sensitivity_CVPR_2024_paper.pdf) | 定位、尺度和误报要共同检查 | 不复制 SLS 损失，不把像素 loss 替换当成结构创新 |
| [ISNet，CVPR 2022](https://openaccess.thecvf.com/content/CVPR2022/html/Zhang_ISNet_Shape_Matters_for_Infrared_Small_Target_Detection_CVPR_2022_paper.html) | 小目标细节应在高分辨率路径处理 | 边缘头、形状监督、正交聚合已有先例 |
| [NS-FPN，CVPR 2026](https://arxiv.org/abs/2508.06878) | 增强细节必须考虑背景噪声 | 不复制小波、低频置信门控和螺旋采样 |
| [PointRend，CVPR 2020](https://arxiv.org/abs/1912.08193) | 细化依赖高分辨率特征，而非仅粗输出 | 不按 p≈0.5 硬选点，以免忽略高置信漏检 |
| [RefineMask，CVPR 2021](https://arxiv.org/abs/2104.08569) | 语义与局部细节协同 | 不照搬 ROI/边界多阶段头；微小目标可能整体都是边界 |
| [FreqFusion，TPAMI 2024](https://arxiv.org/html/2408.12879v1) | 融合应明确区分恢复与噪声风险 | 自适应高低通与相似性采样已有先例；CEL 不替换上采样器 |

保守的候选贡献表述：**面向混合 token 的条件式三角色像素分配，以及在原 CCA 接纳/未接纳特征上作用分离的有界细化。** 两部分共同构成一个模块，需用消融证明耦合的必要性，不凑三个模块。

不能声称“均值守恒上采样”首次提出：守恒超分辨率、均值保持限制器，以及 [USU 归因质量重分配预印本](https://arxiv.org/abs/2603.16067) 都是相关先例。这里可能的区别在于角色条件化、原 skip 门控的作用分离和端到端任务反馈，而不是重命名数学操作。未完成全面相关工作与实证前，不保证顶刊创新门槛或录用。

## 6. 固定协议与启动门槛

- 主工程本地 `datasets/`，各数据集 `img_idx/train_<dataset>.txt` 与 `img_idx/test_<dataset>.txt` 原 train/test（即用户所指原始索引划分）；不新增验证集。
- 初始化 seed42、训练 seed42；从头训练 1000 epoch。batch16，patch256，Adam、学习率及原数据增强继续用既有冻结协议。
- **仅原六头等权 BCE + 原路由损失**。不加 RET/A1、WDS 或新增 H 监督。
- 第 500—1000 epoch 每轮 test，共 501 次；每次完整保存数值指标。
- 原规则分别选 `best_mIoU`、`best_Pd`，分别输出两套权重的完整指标；不拼列，不另加综合分数选第三个模型。
- test 用于逐轮选模，应明确报告 test-selected 的乐观性，不写成未使用测试集选模的无偏结果。固定 seed42 结果不支持多 seed 稳定性主张。
- SCTransNet 使用既有复现参考，不重训，不故意挑更差权重。V3.3 与无 WDS CAST 分开标识；没有 CAST 的 NUDT/NUAA 结果时不拿 V3.3 冒充。

启动前仅检查新的实现风险：概率约束/退化输入/梯度、零增益原模型一致性、真实训练批次更新、state 身份、断点恢复与双 best 导出、实际显存。检查不是新选模规则，不重新做已有全量诊断。

原自动接续合同（已撤销）要求 GPU3 若仍被旧 IRSTD 占用，新任务只允许受限的小批次实现检查；正式 batch16 长训等待旧 IRSTD 正常完成并释放 GPU3。该队列已停止，A1 也没有达到原定的 1000 epoch/501 条记录门槛，不得把停止解读为完成。

## 7. 结果判定与后续实验（不是额外自动启动授权）

第一目标是提升 IRSTD 的 mIoU/nIoU/F1，同时检查 Pd/Fa 是否保住 CAST 的优势。若只改善一部分指标，只报告权衡，不称全面提升。未达标不保证继续堆结构能补齐。

首次完整候选结果出来后，优先做最小机制验证：

1. 相同角色先验直接广播，与学习式块内分配比较，检验空间细化是否必要。
2. 恢复/抑制路径分别关闭的同权重诊断，观察作用方向；这只是机制诊断，不是重新选择 best 权重。
3. 若诊断支持，再按同一冻结协议做独立训练的正式消融，并检验另外两个数据集上的同一模型。

训练集检查只用于排除实现错误，不凭少量训练 loss 判定最终性能。小批次 smoke 权重不能升级成正式初始权重。正式长训前冻结源码和启动记录；运行后不能边训边改。

## 8. 本地证据入口

- SCTransNet：本仓库已跟踪的 `baseline/evaluation/common_evaluator_v1_recheck_20260818/IRSTD-1K.json`；外部 20260901 复核副本与它逐字节相同，不重复发布。
- CAST：外部本地 V3.4-CAST 工作树中的 IRSTD `summary.json`。
- IRSTD A1：外部本地 A1 工作树中的 IRSTD `test_history.json`。
- NUDT A1：外部本地 A1 恢复工作树中的 NUDT `test_history.json`。
- 旧 CAST 配对几何：外部本地 WDS 工作树中的 `geometry_irstd_frozen.json`；目录名含 WDS 不代表其中 CAST 参考结果来自 WDS 模型。
- 新科学实现：独立本地开发工作树中的 `experiments/sctransnet_sbsc_v35_cel.py`。
- 新独立 runner：同一外部工作树中的 `train_sctransnet_sbsc_v35_cel_img_idx_test_selected.py`。

实际检查、源码冻结及已停止的排队状态另见外部本地工作树的 `docs/CEL_IMPLEMENTATION_STATUS.md` 和 `runs/cel_launch/`；它们不在本仓库中，而且本文不能作为“已开始长训”的证明。
