[pytest]
addopts = --strict-markers
norecursedirs = EviSIRST_HF_Decoder_V2_reference
markers =
    artifact: requires the separately published checkpoint artifacts
    integration: exercises checkpoint loading or another multi-component workflow
