# CAST-CIT：预检结果与长训前判定

日期：2026-09-09。状态：独立实现、CPU检查、stage2机制线索诊断及GPU3有界开销预检已完成；尚未创建CIT正式训练入口、启动1000轮训练或产生两类best权重。A1和CEL保持停止。

> **后续状态（2026-09-10 17:33，UTC+8）**：上述是长训前的历史快照。此后正式 runner、
> CUDA 自然激活与断点恢复检查已完成，IRSTD-1K 和 NUDT-SIRST 的 seed42/
> 1000-epoch 运行已启动并分别到 e812 与 e637；运行尚未完成，NUAA-SIRST 尚未接续。
> 当前加固源码、原始预检 artifact 和运行产物均位于未公开的外部本地工作树，
> 未随 `EviSIRST_main` 发布；本文仅是带证据边界的预检摘要。

## 1. 结论先行

**CIT 已获得有限但真实的局部机制依据，值得继续准备单一 stage2 候选的正式实验。** 这比仅凭“填充不足”指定一个修改位置更有依据，但仍不能宣称唯一根因已定位、CIT已修复IRSTD或一定超过baseline。

- 可观察的局部支持：3297个FN像素中，1995个（60.51%）在stage2固定邻域内有同一GT对象的原预测已覆盖像素作为支持线索。
- 可观察的局部分离：原始U/A双路特征的补填候选边/泄漏候选边分数，pooled AUC为0.72094；初始8维随机投影为0.67716。两类有差异但仍重叠，不能把这些数值写成分割性能。
- 接入与开销：真实GPU3、FP32、batch16下，零增益原损失、capture科学字段、所有父参数梯度严格一致；短窗口平均步时约增加2.70%。
- 不覆盖所有问题：1302个FN像素（39.49%）缺少上述支持，其中XDU997占1065个；这类错误不能由本轮局部补全线索解释。

本轮的实验设计技能用于区分“实现正确”“存在机制线索”“正式性能有效”三种证据，没有将其中一种替代另外两种。

## 2. 实际实施了什么

独立工作目录：未随本仓库发布的本地 CAST-CIT 工作树。

- 以无WDS V3.4-CAST为父模型，仅在`up_decoder2`增加CIT；不启用CEL、RET/A1、WDS或新增监督。
- 默认仍为双引导8维、半径1/2、混合上限0.25、零增益、seed42。
- 新增1027参数和4个state keys；总参数11,331,215，state keys共518。
- 补齐审计：实际计算方法替换、投影参数共享/存储别名、参数可训练性、模块拓扑与预期独立参数数量。
- 原CIT数学类和算子函数已与方案附录逐项AST比较一致。未因测试而修改传播公式、初始化、增益上限或标签语义；138份父源码哈希保持不变。

外部本地证据标识为 `experiments/cast_cit.py`、`docs/CIT_PREFLIGHT_PROTOCOL.md` 和
`runs/cit_preflight/completed_freeze.json`。原方案附录仍保留原始代码和 SHA，不冒称它已
包含独立实现中的审计加固。

## 3. CPU与父模型回归

| 范围 | 实际结果 | 边界 |
|---|---|---|
| 新CIT算子、审计与真实CAST接入 | 72项分批通过（先71项，后追加1项非零父CAST gain检查） | 不是原父模型“71项” |
| 原V3.4科学/正式runner/原canary测试 | 77项分批通过 | 不表示运行了canary训练 |
| 新GPU预检安全与分类测试 | 43项通过 | CPU模拟守卫，不冒充CUDA实测 |
| 新stage2几何/边分数测试 | 10项通过 | 不证明机制已有效 |

真实CAST CPU检查包括零CIT gain时六头、6BCE+router三种损失、父参数梯度逐元素相同；父CAST gain非零时也通过。非零CIT gain的CPU检查确认全部CIT参数梯度可达且非零。

父回归初次45项中有1项因独立目录缺少“存在但非授权的数据根目录”而在更早的拒绝条件失败；另44项通过。仅补充不含图像的测试目录README，再单项通过；原路径校验和旧测试代码没有放宽。另32项canary规则测试通过。失败XML与单项复核XML均保留，没有掩盖第一次失败。

## 4. 固定CAST e700的stage2证据

原test201张、297个GT对象，阈值/原标签/父权重不变。每图仅一次父模型原生前向，捕获stage2 U/A；没有安装CIT或生成CIT预测。逐图TP/FP/FN与旧档一致，全部完整性门通过。

### 4.1 邻域支持不是实际恢复率

stage2每cell对应原图2×2区域。对于每个对象，含原预测已覆盖GT像素的cell是支持候选；在CIT固定16邻接偏移内检查FN的同对象支持，按FN像素去重。

| GT面积桶 | FN总数 | 邻域支持FN数 | 占本桶FN |
|---|---:|---:|---:|
| 1–4 | 7 | 0 | 0.00% |
| 5–9 | 19 | 13 | 68.42% |
| 10–25 | 358 | 288 | 80.45% |
| >25 | 2913 | 1694 | 58.15% |
| 全部 | 3297 | 1995 | 60.51% |

同cell支持为990个，本次它们已全部包含在邻域支持集合中，不能再加到1995上。多GT混合cell排除数为0；2×2 cell中正像素在8连通规则下本就相连，不能把该保护无排除宣传成发现。

XDU997没有原预测已覆盖GT像素，故按预声明定义没有donor。它不提供本轮局部补全证据，但“没有最终输出donor”不能反推隐藏特征没有任何目标信息，也不是正式test删图依据。

### 4.2 候选边的特征分离

同对象支持cell到FN cell作为补填候选边；支持cell到完全GT空且四像素均有效cell作为背景泄漏候选边。每图各类无向边去重，不使用GT更新模型或拟合分类器。

| 固定分数 | 补填边均值 | 泄漏边均值 | pooled边加权AUC | 逐图宏平均AUC |
|---|---:|---:|---:|---:|
| 原64维U/A，同式归一化与精度2 | 0.20794 | 0.09121 | 0.72094 | 0.70850 |
| CIT seed42初始8维随机投影 | 0.30547 | 0.17065 | 0.67716 | 0.64720 |

共同分母：6313条补填候选边、28032条泄漏候选边；宏平均只覆盖同时有两类边的177张图。AUC为正类分数高于负类的概率，并列记0.5。这是事后测试集诊断，不是新选模指标。

结果支持父特征存在一定局部分离线索，但也提示8维随机投影初始分离弱于原特征。不能据此直接移除可学习投影，也不能宣称它学习后一定更好。亲和性差异不等于传输后的TP/FP变化，原卷积/非线性还会影响方向。

## 5. GPU3训练接口和成本

精确复用旧manifest中的64个训练增强样本，四批×16、patch256、增强epoch0、seed42；sample ID、增强计划、图像/mask张量SHA逐项一致。优化器按原训练epoch1配置：FP32、Adam、lr=0.0001，原CAST投影保留并追加CIT投影。没有访问test、加载旧训练权重或导出预检权重。

零增益门：共享初始状态、总/分割/路由loss、capture科学字段及435个父参数张量的梯度状态均严格相同。两个实例的module_id先核对各自归属，再仅为跨实例比较规范化此字段；未丢弃capture科学数值。

| 四步计时短窗口 | 父CAST | CIT | 相对变化 |
|---|---:|---:|---:|
| 平均完整计算步 | 0.9710 s | 0.9972 s | +2.70% |
| 峰值allocated | 7.7650 GiB | 7.7606 GiB | −4.58 MiB |
| 峰值reserved | 11.1230 GiB | 11.1563 GiB | +34 MiB |

计时各自从seed42新建，先2步热身，再固定四批计时；包含forward、原loss、backward、Adam和投影，排除数据传输。父/CIT顺序运行。样本窗口、热状态和早期gain范围有限，不能宣称稳定长训仅慢2.7%，也不把微小allocated差异称为显存优化。正式训练后期的CAST状态与显存峰值仍可能变化。

### 增益启动的准确解释

- 独立启动检查的前两次更新，gain均被投影到0；此时引导参数梯度为0，但连接与有限性正常。
- 在另一个预先安排的fresh成本窗口，第3次更新后gain自然到`3.63577e-6`，第6次更新后到`1.96115e-4`。没有增加步骤或强制非零初始化来促成它。
- 顶层`branch_activation_observed=false`仅描述前两步的完整gain+guide检查，不能解释为分支始终没启动。成本窗口只额外记录gain，没有逐项保存启动后guide梯度；不能由此宣称全部引导已充分学习。

`all_gates_pass=true`仅表示实现完整性与成本窗口完成，不是模型性能门，也没有自动授权长训。

## 6. 现在可以推进到哪里

可以准备独立CIT正式runner和CIT专用checkpoint身份，复用原训练/选择规则；需完成真实CUDA非零gain后的引导梯度记录，以及optimizer/RNG/断点恢复/正确out/两类best导出的最小接口检查。可在同一次runner smoke中完成，不再重复旧几何或展开超参数网格。

之后才开展单一stage2候选正式实验：原始train/test，初始化及训练seed42，1000 epoch，第500–1000轮逐轮test，best_mIoU与best_Pd分别保存和完整报告，同时对照既有SCTransNet与无WDS CAST。不新增验证集或多seed，不把预检状态用作正式初始化，不默认重训baseline。

本轮没有创建正式runner或自动服务，CIT尚无mIoU/nIoU/F1/Pd/Fa正式成绩。两类best权重均为“尚未训练产生”，不使用空模板或诊断值冒充结果。GPU3预检已退出；A1/CEL服务仍inactive/dead，旧权重保留。

## 7. 原始证据入口

- `stage2_evidence.json`：stage2 完整逐图/逐对象结果；
- `cuda_preflight.json`：真实 GPU 四批接口与成本结果；
- `cpu_parent_v34_regression.xml`：原回归首次结果，含 1 项目录前提失败；
- `cpu_parent_path_fixture_recheck.xml`：该单项复核；
- `cpu_parent_canary_regression.xml`：原 canary 规则回归；
- `cit_native_nonzero_cast_gain.xml`：新 CIT 的非零父 CAST gain 单项 CPU 结果。

上述原始产物保留在外部本地工作树，未收录在本次 Git 发布中。

其余CPU测试按本轮执行日志分批完成，未为合并XML重复全部测试；新增测试代码与源码哈希均保留在独立目录。
