# EviSIRST CAST-RET：核对结论与冻结实验约束

> **后续状态（2026-09-09）**：本文是 2026-09-08 的历史审阅和冻结协议。其中指出的
> 真实接口、灰度标签和 runner 阻断项已在后续独立 A1 工作树中完成修订、预检及
> 恢复一致性检查；IRSTD 和 NUDT 的本地正式运行已启动，但尚无最终测试指标或 best
> 权重。当前 `main` 仅发布文档，未收录 A1 源码、完整预检产物、运行日志或权重。

**审阅对象：** `EviSIRST_CAST_RET_IRSTD_Plan.md` 及其代码附录<br>
**整理日期：** 2026-09-08<br>
**文档状态：** 本地复核后的冻结协议与修订边界；候选接入检查不等于正式训练已通过。

> **总体判断：用户提出的意见成立。CAST-RET 的方向可以保留，但当前附录不应直接用于正式训练。**
>
> 接口与灰度标签是已经指出明确冲突的兼容性问题；新增损失的梯度强度和训练开销是有实现依据、但尚未量化的风险；同源错误权重的联动收益与论文创新性仍有待实验支持。不能把这几类结论混为一谈。

## 1. 证据范围与判断总表

初版整理依据已交付方案文件、此前的核对意见及用户提供的本地事实，没有直接取得真实 V3.4 代码和数据。2026-09-08 后续本地复核已读取无 WDS V3.4 工作树、原训练索引、已有统一复评与几何记录，并核对原方案 SHA256 与附录 A 一致。下文区分本地代码/数据证据、CPU 候选接入检查和仍未完成的正式实验，不将其中一种替代另一种。[L1][L2][L3]

| 用户的判断 | 核对结论 | 证据边界 |
|---|---|---|
| 真实路由记录在 `capture.router.records`，附录接口不匹配 | **正确，是正式训练阻断项** | 已直接核对本地 V3.4 类型与损失函数；附录确实访问 `capture.records`。[L1][A1] |
| 原函数第五项已经是 CAST capture，不能替换成 RET 统计 | **正确，是原审计契约问题** | 已核对原生五项返回与调用方；附录第五项确实是 `diagnostics`。[L1][A1] |
| IRSTD 存在灰度标签，RET 的严格二值要求不兼容 | **正确，是确定的输入契约冲突** | 已核对原 loader 和原 train800 mask；附录明确拒绝任何非 0/1 值。[L2][A2] |
| 不能全局二值化，必须保留原 BCE 与原路由标签语义 | **正确** | 全局改标签会同时改变原有监督，破坏单独评估 RET 的比较边界。[U1][S1][S2] |
| 面积归一化后，系数 0.1 不代表梯度扰动小 | **完全正确** | 可由附录权重公式直接推导；但不能据此断言总参数梯度一定失控。[A3] |
| 逐图 CPU 连通域及重复同步可能拖慢训练 | **实现依据成立，减速幅度未知** | 附录包含逐图 `.cpu().numpy()`、回传设备、`nonzero()` 与完整排序；尚无真实耗时测量。[A3][S4] |
| 同源像素错误权重连接分割与路由，是值得检验的核心 | **正确，但属于候选机制** | 附录的实际联动主要发生在困难背景分支，不是所有正负像素误差都共享同一权重。[A4] |
| 尚未证明性能收益或论文创新 | **正确** | 没有完成真实 CAST＋RET 的 IRSTD 正式训练与归因实验。 |
| 已有几何诊断可以复用，不必全部重跑 | **正确，有适用条件** | 权重、数据列表、评估与诊断定义一致的旧诊断可复用；新增 RET 特有的检查仍需补充。 |

## 2. 后续正式实验协议：按用户要求冻结

以下约束优先于旧方案中的冲突建议。不能再以修订 RET 为由，新增验证集、多 seed 实验或第三种选模角色。

| 项目 | 冻结要求 |
|---|---|
| 数据划分 | 使用原始 `train/test` 划分，不从训练集另划验证集。 |
| 数据来源 | `<workspace>/EviSIRST_main/datasets` 下各数据集的原 `img_idx/train_*.txt`、`img_idx/test_*.txt`；不改列表。 |
| 模型初始化 | 固定 `seed42`。 |
| 训练随机性 | 固定 `seed42`，不做多 seed 实验。 |
| 正式训练长度 | **1000 epoch**。 |
| 测试频率 | 从第 **500 epoch** 起，每个 epoch 执行一次 test，直到第 1000 epoch。 |
| 测试窗口 | 按 epoch 从 1 开始计数，为第 500—1000 epoch，首尾均包含，共 **501 次**评估。 |
| 选模角色 | 只保留 `best_mIoU`、`best_Pd`。 |
| 选择与并列处理 | 继承本地原选择器的既有规则，包括同分处理；不暗中改变。 |
| 完整结果 | 每个角色分别报告其对应单一 checkpoint 的 epoch、mIoU、nIoU、F1、Pd、Fa。 |
| 指标比较 | 同角色对同角色，不跨 epoch、权重或角色拼接五项指标。 |
| SCTransNet 参考 | 复用已确认身份的 baseline 权重与复评；不新增 baseline 训练。历史权重角色不明确时按实际 checkpoint 身份列为参考，不虚构双 best。 |

这里的“双 best”限制选模角色，不禁止保存原有恢复状态、逐 epoch 指标、配置与审计记录。

### 2.1 旧方案中必须撤销或改写的要求

原方案中“新增训练/验证划分”“至少三个配对 seed”“新增 `best_feasible`”均与本次冻结协议冲突，不再作为后续执行要求。[A5]

尤其不能先按 Pd/Fa 约束筛选一批 checkpoint，再在其中选择最高 mIoU，却仍称为原来的 `best_mIoU`。这实际上已经改变了选模规则。正确做法是：**按原双角色规则选出权重，再完整分析各项指标的改善与退化；结果分析不得反过来改变被报告的权重身份。**

旧方案中的任意数值晋级阈值，也不能自动成为用户已经确认的约束。本文件不新增阈值、复合评分或第三种角色。

### 2.2 协议可执行，但解释边界必须保留

使用 test 集逐轮选择 checkpoint，结果应准确描述为 **test-selected**，不能称为完全未参与选模的独立测试。固定 seed42 可以支持该固定设置下的比较，但单 seed 结果不能支持跨 seed 稳定性或多次重复实验显著性结论。公开历史训练入口与选择器也对固定 seed、后半程测试选模作了相应约定。[S1][S3]

这些是报告边界，**不是要求新增验证集或更改实验协议**。

接口、数值与有限步优化检查可以在正式训练前开展，但不能用短程检查结果替代 1000 epoch 的正式性能比较，也不能把诊断批次包装成新增验证集。

## 3. 接口审阅：不只是改一个属性名

### 3.1 附录中的冲突可以直接定位

原方案附录的适配器读取：

```python
records = getattr(capture, "records", None)
```

并返回：

```python
return total, segmentation, router, breakdown, diagnostics
```

以上均为原附录摘录，不是修复后的建议代码。[A1]

用户确认的本地契约则是：

```text
路由记录：capture.router.records
原函数第五返回值：CAST capture
```

因此存在两个独立问题：访问错误的记录层级，以及占用了原 capture 的返回位置。只修复前者，后者仍会破坏下游调用或审计。

### 3.2 必须保留的契约

接入 RET 时，应保留原生 capture 的生命周期、类型、对象身份及第五返回值语义。RET 的标量与统计可以通过明确的独立日志通道或其他经调用链核对的方式输出，不能直接拿 `diagnostics` 替代 capture。

本地原生路径还包含两项不能遗漏的要求：[L1]

- 同一次 forward 的 `capture.router` 与 `capture.cast` 均保留，并继续执行 `validate_c3_v34_training_capture(capture)`；缺失 CAST 分支、行覆盖或求解计数错误仍必须被拒绝。
- 原 `router_loss_v33`（V3.4 复用的原路由损失）的参数是 `capture.router`，不是完整 capture。修正 `.records` 访问并不能替代这一修正。

也不能未经检查就简单“增加第六返回值”：调用方可能使用固定长度解包。是否扩展返回结构，必须以真实调用链为依据，而不是由附录自行假定。

公开 V3.3 的函数接口不能作为本地 V3.4 接口的替身；旧附录中用 `SimpleNamespace(records=[])` 构造的测试对象，只能证明适配器满足了这个模拟对象，不能证明其适配真实 CAST。[A6][S2]

**本项结论：正式训练前必须修复。不能通过禁用审计、绕过身份检查或丢弃 capture 来获得“能跑”的表象。**

### 3.3 正式训练入口的损失分解也需要独立适配

现有无 WDS runner 会检查 `total == segmentation + router`。启用 RET 后应明确区分原 segmentation、原 router、RET 新增项与 total，并设计对应日志/恢复身份；不能将新总损失塞进原入口后，删除这个检查而不建立替代契约。[L1]

候选适配器可以调用原生损失函数并通过临时 forward hook 取得同一次 forward 的输出，保留原五项返回，将 RET 统计送入独立字典。该方式不构成正式 runner 已接入；正式日志、恢复与来源绑定仍需单独验收。

## 4. 标签审阅：保留原监督，单独定义 RET 语义

### 4.1 确定的兼容性问题

原附录 `_check_binary` 包含：

```python
if not bool(torch.isfinite(x).all()) or bool(((x != 0) & (x != 1)).any()):
    raise ValueError(f"{name} must contain only 0/1")
```

这不是阈值转换，而是对所有非 0/1 标签直接报错。[A2]

结合用户已确认的 IRSTD 灰度训练 mask，当 RET 启用并处理到含中间灰度值的批次时，附录不能按原样接受数据。原方案的单元测试通过，并不能消除这个真实数据契约冲突。

### 4.2 为什么不能全局二值化

原训练语义是归一化后的原始 mask；公开入口标记了 `raw_mask_div_255`，原路由监督也基于保留灰度信息的标签构造目标。[S1][S2]

如果在全局把 mask 改成二值再同时送入六头 BCE、原 router loss 和 RET，实验实际改变的就不止 RET，还包括原分割监督和原路由监督。后续变化将无法清楚归因于新机制。

因此应区分两个职责：

```text
原始归一化训练标签
    ├─ 原六头 BCE：保持不变
    ├─ 原路由监督：保持不变
    └─ RET 专用标签/权重构造：另行定义，不回写原标签
```

这是语义边界，不是已经完成的实现。

### 4.3 不应立即把某个辅助规则定为正式方案

以下都是需要核对的候选，不能仅为了让程序通过检查而直接选定：

| 候选规则 | 必须核对的问题 |
|---|---|
| 非零灰度都视作 RET 正例 | 是否扩大对象支撑域、改变边界与实例面积，是否与原标签用途一致。 |
| 固定阈值生成 RET 二值标签 | 阈值是否删除弱标注目标或改变连通性，是否有数据依据。 |
| 只使用恰为 0 或 1 的像素，中间灰度不参与 RET | 无纯 1 像素的目标是否失去正例修复，灰度边界是否被系统性排除。 |

此前直接提出“RET 只使用恰为 0 或 1 的标签像素”过早。它可以作为保守候选，但尚不能定为正式规则。灰度值也不能在没有数据依据时被解释成“不可靠标签”。

后续至少要明确 RET 的实例构造、正像素恢复、近邻/远背景划分、含目标 token 保护四者如何使用标签，并核对其一致性。尤其不能把 RET 没有选入正例的灰度目标区域，未经论证地当作可抑制背景。

**本项结论：必须修复标签接口，但不应通过全局二值化修复，更不能在尚未核对语义时把某个 RET 阈值规则写成既定答案。**

### 4.4 本地数据核对后的候选接入规则

原 IRSTD train800 的 209,715,200 个像素中，发现 118 个非 0/255 灰度像素，涉及 72 张图。其中低于 128 的 60 个，高于或等于 128 的 58 个。按 8 连通计数，`raw > 0` 为 1198 个组件，`raw > 127.5` 与 `raw == 255` 均为 1195 个，且后两者逐图组件数一致。仅 `raw > 0` 新增的 3 个无 255 组件为孤立灰度像素；不能据此将其定性为噪声。[L2]

据此，本轮候选接入显式使用 `eval_half_soft_positive_zero_background_guard_nonzero_v1`：

| 用途 | 候选规则 |
|---|---|
| 原六头 BCE、原 router | 原 `raw_mask / 255` 原样保留。 |
| RET 实例几何、实例面积权重 | 增强/裁剪后的 `y > 0.5`，8 连通。 |
| RET 正例恢复的 BCE 标签 | 上述几何内部仍使用原 soft `y`，不强制改为 1。 |
| RET 可采样背景 | 仅 `y == 0` 且 RET valid 的像素。 |
| 近邻/远区分界、含目标 token 保护 | 使用所有 `y > 0` 的支撑域，低灰像素也不作为可抑制背景。 |
| `0 < y <= 0.5` 的灰度像素 | 原监督不变，不参与 RET 正/负像素修复；仍参与保护。 |

这使实例几何与 evaluator 阈值一致，同时避免将被辅助正例规则排除的灰度像素新增当作负例。配置必须显式声明该规则，不能静默套用二值阈值。全图统计不能代替增强后连通域：静态几何应在 CPU 的增强后 GT 上构建，再与原标签一起传入设备，并验证二者绑定。

这是有数据依据的**候选实现语义**，不是灰度标注意图已被证明，也不是正式性能配置已经完成验收。RET valid 只限制新增项，不改变原 BCE/router 的 padding 处理。

候选准备器明确接受原 loader 的 float32 标签，拒绝隐式降低标签精度；几何完整性同时检查对象/版本绑定与 detached 状态。[L4]

## 5. 梯度审阅：0.1 不是“小扰动”的证明

### 5.1 面积归一化改变了监督份额

设一张图有 \(S=H\times W\) 个像素、\(M\) 个目标，第 \(j\) 个目标面积为 \(A_j\)。对相同的硬正例标签，忽略相同的 batch 平均因子，原最终头平均 BCE 对每个像素分配的权重是：

\[
w_{\mathrm{BCE}}=\frac{1}{S}.
\]

附录按实例恢复项对第 \(j\) 个目标内每个像素的权重是：

\[
w_{\mathrm{RET}}=\frac{\lambda}{M A_j}.
\]

因此，孤立比较这两项在同一输出像素上的直接贡献，有：

\[
R=\frac{w_{\mathrm{RET}}}{w_{\mathrm{BCE}}}
 =\frac{\lambda S}{M A_j}.
\]

这是由附录公式得到的推导，不是梯度实测结果。[A3]

### 5.2 数值例子与解释限制

仅作公式示例：若图像为 \(256\times256\)，有一个面积为 10 像素的目标，且 \(\lambda=0.1\)，则：

\[
R=\frac{0.1\times65536}{1\times10}=655.36.
\]

这说明：新增正例恢复项在这些像素上的直接权重，可以远高于原最终头平均 BCE。**它不表示整个模型的参数梯度放大了 655.36 倍。** 原训练还有其余五个监督头、原路由损失，不同分支对共享参数的传播路径也不同。

同样，不能由此直接判定实例归一化错误。提升小目标的监督份额本来就是这一设计的目的。需要撤回的是“系数只有 0.1，所以影响已受限、扰动较小”的推断。原文把 `0.10` 描述为“限制新增监督影响”，依据不足。[A3]

### 5.3 正确的风险表述

> 新增项可能显著改变梯度分配；当前尚未证明其强度适合无 WDS CAST，也尚未证明它必然压制原机制或降低性能。

后续应在固定训练诊断批次上核对原损失与 RET 的梯度量级、方向关系及有限步更新，而不是只看损失标量或外部系数。此类检查不需要增加验证集，也不构成多 seed 实验。

## 6. 效率审阅：代码依据明确，速度结论仍待测量

原附录在训练损失路径中逐图执行：

```python
labels_np, count = ndimage.label(
    y[b, 0].cpu().numpy(), structure=np.ones((3, 3), dtype=np.uint8)
)
labels = torch.as_tensor(labels_np, device=p.device, dtype=torch.long)
```

随后还执行区域 `nonzero()`、`torch.argsort(..., stable=True)`，并包含多处把设备张量转换为 Python 布尔值的检查。[A2][A3]

在 GPU 训练路径上，这些操作包含设备到主机的数据读取、CPU 连通域计算、标签传回设备及可能的主机同步。PyTorch 官方性能指南也将 `.cpu()`、`.item()`、CUDA `nonzero()` 等列为需要注意的同步操作。[S4]

因此应准确区分：

| 可以成立的结论 | 目前不能成立的结论 |
|---|---|
| 训练热路径存在额外 CPU 工作、数据搬运、同步与排序开销。 | 已经测得训练明显变慢或某个具体百分比的减速。 |
| 实现需要在正式训练前处理并测量。 | 只要没有新增网络参数，就没有额外训练开销。 |
| 缓存、减少重复检查或重构计算路径值得考虑。 | 原图连通域缓存可以不加核对地替代增强、裁剪后的实例定义。 |

缓存必须考虑增强和裁剪对实例面积、可见区域及连通性的影响。排序优化也应保留声明过的并列处理与确定性语义，不能为了加速悄悄改变被选中的困难像素。

**本项结论：用户指出的性能风险正确，但应保留“尚未量化”的限定。CPU 单元测试也不能代替 CUDA/AMP 集成与真实训练耗时测量。**

## 7. 核心机制审阅：值得检验的是同源联动，而不是新名称

### 7.1 附录的实际贡献范围比口号更窄

原方案各项权重的关系如下：[A4]

| 损失部分 | 使用的权重或构造 |
|---|---|
| 正例恢复 `L_obj` | 独立的实例归一化正像素权重。 |
| 背景修复 `L_tail` | 近邻与远背景困难像素形成的负像素权重 \(w^-\)。 |
| 路由修复 `L_R` | \(w^-\) 经预测置信度调制、token 投影及目标保护过滤后的权重。 |

因此，更准确的机制描述是：

> **用困难背景误差的同源权重，连接最终分割的背景修复与困难背景加权的 H 路由强化监督。**

“同源”不等于两个分支最终持有逐项完全相同的权重张量，也不等于正例恢复已经被纳入同一条联动链。

`L_R` 实现为加权的 `-log p_H`，不是“原路由预测已匹配教师便归零”的残差。例如纯背景 token 的原教师为 `(0, 0.8, 0.2)` 且路由与其匹配时，原 CE 对 logits 的梯度为零，而新增 H-CE 仍会继续提高 H 的份额。应将其解释为原有错误信息的选择性强化，不能宣称新增了独立教师信息。

### 7.2 完整 RET 优于锚点，不足以证明联动有效

即便完整 RET 的最终结果优于无 WDS CAST，仍可能存在不同解释：收益来自独立像素修复、来自附加路由监督，或仅仅来自最终头监督加强。

因此，若要主张“共享错误权重带来额外价值”，需要能区分这些解释的对照。完整 RET 与仅像素修复、仅路由修复、额外最终头 BCE 等对照有分析价值；但即便组合优于单项，也不自动证明收益专门来自“共享”而非两种损失的简单叠加。

这些是归因要求，不是新增 checkpoint 角色。任何正式对照仍应遵守原 train/test、seed42、1000 epoch、第 500 epoch 起逐轮 test，以及两个原选模角色。

在没有结果之前，不应宣称 RET 提升 IRSTD 性能、解决 Pd/Fa 交换，或补足论文创新。相反，也不能仅凭当前附录的工程问题否定整个方向。

**目前恰当的定位是：有针对性的待验证训练机制，不是已经成功的优化。**

## 8. 已有诊断怎么复用

不需要为了修订附录，把已经有效的几何诊断全部重跑。但应清楚区分可继承证据和新机制必须补充的证据。

| 诊断内容 | 处理原则 |
|---|---|
| 旧权重的漏检、目标覆盖不足、匹配组件总外溢、未匹配组件分布 | 权重身份、数据列表、评估与诊断规则一致时，可继续复用。 |
| 外溢的近邻/远区像素拆分 | 现有几何报告未计算，不能把总外溢当作近邻外溢；需要按固定空间定义补充。 |
| 旧权重的完整指标与逐图结果 | 来源和口径可追溯时，可作为对应旧权重证据；不跨角色拼接。 |
| 原 CAST 的路由模式与回退情况 | 可作为旧机制参考，不能直接代替接入 RET 后的审计。 |
| RET 标签有效覆盖率与被排除区域 | 需要基于最终辅助标签规则补充。 |
| RET 的共享错误权重及 token 保护后剩余权重 | 需要补充；旧模型没有这一机制。 |
| 新增梯度强度、capture 完整性、CUDA/AMP 行为、训练耗时 | 需要对真实接入版本补充。 |

“复用已有诊断”不是“跳过新代码检查”，而是避免重复生成已经有效的旧模型证据。

已有 `WDS_GEOMETRY_FINDINGS.md` 虽位于 WDS 工作树，其实际来源是 SCTransNet e713、无 WDS CAST best_mIoU e700 与 best_Pd e531，可按对应身份复用。“共同目标多缺失 164、少外溢 110”中的外溢为匹配组件总外溢，不是近邻/远区分解。相反，`WDS_LOSS_DIAGNOSTIC.md` 的梯度数值确实来自 WDS e75，不能用于证明无 WDS CAST 的梯度问题。[L3]

## 9. 正式训练前的修订边界

本节是进入正式训练前的工程条件，不是第三种选模规则，也不预设性能会提升。

| 优先级 | 必须完成的工作 | 完成标准 |
|---|---|---|
| P0：真实接口 | 按本地 `capture.router.records` 访问路由记录；原路由损失接收 `capture.router`；保留第五返回值与完整 `.router/.cast`。 | 原 `validate_c3_v34_training_capture` 仍执行且能拒绝缺失/错误 CAST 记录，不依赖模拟对象替代。 |
| P0：标签语义 | 原 BCE、原 router 标签不变；明确 RET 专用构造及含目标 token 保护。 | 灰度 mask、弱标注目标及混合 token 的行为有明确、可检查的定义。 |
| P1：零改动基线 | 在同一模型状态、输入和随机状态下，关闭 RET 或令其贡献为零。 | 原损失、参数梯度及一步优化器更新与原训练路径一致，capture 保持完整。 |
| P1：启用路径 | RET 系数大于零时处理含灰度像素的输入，并覆盖非零 CAST gain。 | 有限 loss/梯度、原标签未改写、原生校验及单次 forward 成立；零贡献通过不能代替此项。 |
| P1：梯度量级 | 核对新增项与原损失的量级和方向关系。 | 不再只用 `0.1` 作为“小扰动”的依据；检查结果可追溯。 |
| P1：训练实现 | 处理逐图 CPU 连通域、重复同步与排序开销，保留监督语义。 | 有真实接入版本的耗时、内存及必要数值检查结果。 |
| P1：正式入口 | 明确四项损失分解、RET 统计、来源/配置身份、恢复状态和原双 best 选择器。 | 新入口不误用旧 runner 的三项总和假设；仍执行原训练与测试协议。 |
| P2：正式比较 | 按冻结协议运行并保存两个原角色。 | 两个角色各有完整、同一 checkpoint 的五项指标。 |

此前原文报告的 24 项 CPU 单元测试，并未验证本地 capture 契约或真实灰度数据兼容性。本次没有重新运行这些测试，也没有据此宣称代码已经修复。

2026-09-08 已在独立候选工作树完成新增的 74 项 CPU 回归（61 项辅助模块、8 项真实 CAST 接入、5 项原协议回归），全部通过。这里的真实接入使用完整 CAST 和含灰度/空前景的合成输入，证明 CPU 契约与零贡献等价，不证明正式性能或 CUDA/AMP 行为。原方案附录仍原样留存，不能与新候选源码混称；在本阶段正式 runner 尚未接入，后续 A1 阶段已完成该工程项。[L4]

## 10. 双角色报告格式与最终结论

正式结果应至少按下表分开保存。以下为格式模板，不包含虚构实验值：

| 方案 | 权重角色 | Epoch | mIoU % | nIoU % | F1 % | Pd % | Fa ×10⁻⁶ ↓ |
|---|---|---:|---:|---:|---:|---:|---:|
| SCTransNet | 冻结历史 checkpoint 参考；双 best 身份未确认 | 713 | 67.7657 | 67.1461 | 80.7862 | 93.2660 | 20.8005 |
| 无 WDS V3.4-CAST | `best_mIoU` | 对应原记录 | 对应原记录 | 对应原记录 | 对应原记录 | 对应原记录 | 对应原记录 |
| 修订后 CAST-RET | `best_mIoU` | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |
| 无 WDS V3.4-CAST | `best_Pd` | 对应原记录 | 对应原记录 | 对应原记录 | 对应原记录 | 对应原记录 | 对应原记录 |
| 修订后 CAST-RET | `best_Pd` | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 | 待实测 |

前四项的差值使用百分点，Fa 的差值使用 ×10⁻⁶；Fa 增加表示变差。角色间不能拼接最优值，一个角色的改善不能掩盖另一个角色的退化。

SCTransNet 行来自已完成的 IRSTD test201 统一复评。[L3] CAST/RET 两个角色各自与该冻结参考完整比较，但不能将这一历史权重伪称为 baseline 的两个选择角色。若后续确认了 baseline 双 best 的原始记录，可按真实角色补列，无需为补表重新训练 baseline。超过无 WDS CAST 只证明 RET 的增量，不能替代超过 SCTransNet 的目标。

**本阶段结论：** 用户的初步判断总体正确。应停止把当前附录视为可直接启动正式训练的实现，但可以保留 CAST-RET 方向。本文当时冻结的下一步是先修复真实接口、辅助标签语义和训练实现，再检验梯度与开销，最后在冻结的 seed42 双角色协议下验证性能；后续工程状态以文首更新与 A1 入口文档为准。既不能因为部分 mIoU 上升就称优化成功，也不能把尚未量化的风险写成已发生的失败。

---

## 附录：证据索引

### A. 本次重新读取的原方案文件

文件：`EviSIRST_CAST_RET_IRSTD_Plan.md`。

本次读取版本的 SHA256：

```text
ee3308b99e74f3e0a87e57d2fb033d6bb87fab2c2483e5a8a085624ce1fa7367
```

以下行号属于该 Markdown 文件，不是用户本地 Python 文件行号；原文件改动后行号可能变化。

| 编号 | 位置与内容 |
|---|---|
| [A1] | 附录 A，`training_losses_with_native_cast`；约第 767—800 行，含错误层级访问及第五返回值 `diagnostics`。 |
| [A2] | 附录 A，`_check_binary` 与 `build_error_ledger`；约第 567—591 行，含严格二值检查和 Python 布尔值检查。 |
| [A3] | 正文第 4.2 节与附录 A，`build_error_ledger`；约第 604—638 行，含 CPU 连通域、实例归一化、`nonzero()` 和稳定排序；正文约第 224—231 行给出 0.10 配置及其原解释。 |
| [A4] | 正文第 4 节与附录 A 的 `ErrorLedger`、`project_ledger_to_tokens` 及修复损失实现，定义正例、背景及路由三部分。 |
| [A5] | 正文第 6.3—6.4 节，约第 359—387 行，含新增验证集、多 seed 与 `best_feasible` 建议；这些建议已被本次用户约束取代。 |
| [A6] | 附录 B，约第 1031 行，模拟接口使用 `SimpleNamespace(records=[])`；不是本地 CAST capture。 |

### U. 用户提供的本地事实与约束

[U1] 本轮用户说明：真实记录路径为 `capture.router.records`，原函数第五返回值是 CAST capture；IRSTD 训练 mask 确实包含灰度标签；原始 train/test、初始化与训练固定 seed42、1000 epoch、第 500 epoch 起逐轮 test、只保存并报告 `best_mIoU` 和 `best_Pd`；已有几何诊断可复用。

这些约束是执行依据；具体本地事实在后续核对中已有下列本地来源支持，不能把未完成的真实联调或正式训练也写成已完成。

### L. 2026-09-08 本地复核来源

- [L1] 无 WDS V3.4 工作树 `<workspace>/EviSIRST_v34_cast_20260903`，干净基准 commit `99e3bed123339ea5e75f84c4430182444be29fc7`。`experiments/sctransnet_sbsc_v34_cast.py` 的 `C3V34TrainingCapture`、`training_losses_v34` 与原验证器；`train_sctransnet_sbsc_v34_cast_img_idx_test_selected.py` 的 `_training_losses`。二者 SHA256 分别为 `71a3e3f176c04d9870645bc3b4114c85446edbc214a654800108ce61d3eae807`、`64d46384bb81a69e7232e1a3a694810fa82ef1efba63918fbca288513a3ea657`。
- [L2] `<workspace>/EviSIRST_main/datasets/IRSTD-1K/img_idx/train_IRSTD-1K.txt` 对应的 800 张原始 mask；原 loader 保留 `raw_mask / 255`。本次图像级统计与候选标签边界记录在隔离接入工作树的 `docs/CAST_RET_LABEL_AUDIT.md`，不能代替增强后样本或正式训练验证。
- [L3] `<workspace>/EviSIRST_v34_wds_20260907/WDS_GEOMETRY_FINDINGS.md`、`runs/sbsc_v34_cast_wds_v1/geometry_irstd_frozen.json` 和 `WDS_LOSS_DIAGNOSTIC.md`；分别按其中记录的真实权重来源使用。现有无 WDS CAST 正式 summary 位于 `<workspace>/EviSIRST_v34_cast_20260903/runs/sbsc_v34_cast_img_idx_test_selected/formal/sbsc_v34_cast/IRSTD-1K/run_seed_42/summary.json`。
- [L4] `<workspace>/EviSIRST_v35_cast_ret_20260908/docs/CAST_RET_INTEGRATION_STATUS.md` 与该树 `artifacts/cast_ret_cpu_checks_20260908_final.xml`；记录新增候选源码的 SHA256、实际 CPU 测试结果和未完成边界，不是原附录 24 项测试的复现声明。

候选接入目录：`<workspace>/EviSIRST_v35_cast_ret_20260908`。它从 [L1] 基准独立派生，不修改旧 CAST/WDS 冻结代码、权重或数据。开发目录名称不是已经完成的正式模型版本证明。

### S. 上一轮核对意见所引用的公开来源

以下来源作为原讨论的参考索引保留。本次文件整理没有重新抓取网页，因此不将其视为对仓库当前最新状态的新一次核验；公开 V3.3 接口尤其不能替代本地 V3.4 契约。

**[S1] 公开 V3.3 训练入口：** 原始标签规则、seed 与训练/测试选模约定。

```text
https://raw.githubusercontent.com/Arialliy/EviSIRST_main/main/train_sctransnet_sbsc_v33_img_idx_test_selected.py
```

**[S2] 公开 V3.3 实现：** 原路由监督与训练损失接口。

```text
https://raw.githubusercontent.com/Arialliy/EviSIRST_main/main/experiments/sctransnet_sbsc_v33.py
```

**[S3] 公开历史双角色选择器：** `best_mIoU`、`best_Pd` 与 test-selected 语义。

```text
https://raw.githubusercontent.com/Arialliy/EviSIRST_main/main/experiments/sbsc_v33_test_selection.py
```

**[S4] PyTorch 官方性能指南：** CPU/GPU 同步与训练热路径开销。

```text
https://docs.pytorch.org/tutorials/recipes/recipes/tuning_guide.html
```
